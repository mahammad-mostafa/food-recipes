<div class="block">
  <h2>Welcome <%= current_user.name %></h2>
  <p>Currently, you have a total of <%= @recipes %> recipes & <%= @foods %> food items.</p>
  <%= link_to('Logout', destroy_user_session_path, data: { turbo_method: :delete }) %>
</div>