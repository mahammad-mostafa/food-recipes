<h2>All recipes</h2>
<section>
  <% if @recipes.length.zero? %>
    <%= render(partial: 'partials/empty', locals: { name: 'recipes' }) %>
  <% else %>
    <%= render(partial: 'recipe', collection: @recipes, locals: { public: true }) %>
  <% end %>
</section>